# Workflow State — KED Site Assessment

*Session opened: 2026-07-24 (backfilled at end-of-session, bootstrapping this file from prior session history) | Phase: ANALYZE*
*Read this file at the start of every response. Update it at the end of every response.*
*One active session writes to this file at a time.*
*Cross-session prefs live in agentmemory (dev runtime), not here. Optional narrative archive → `_docs/sessions/`.*

---

## State

```yaml
phase: CONSTRUCT
status: COMPLETE
current_task: "Consolidated the regional/neighborhood-scale illustration prototypes (step 4) into committed, reusable, tested scripts and documentation. New: docs/ILLUSTRATION_NOTES.md, docs/DESIGN_SYSTEM.md (finished-output visual design, separate from illustration-content decisions), R/illustrate/{regional_inset,neighborhood_context,basemap_tiles}.R. Built a scroll-simulation mockup (Artifact, not committed to repo) testing the two prototypes in sequence - Peter confirmed the styling direction is worth preserving. Refreshed NEW_SESSION_PROMPT.md, stale since 2026-07-24."
blocked_by: null
next_action: "Step 5: parcel-scale illustration - the new focus per Peter. DEM raster work (hillshade/slope/TPI), reserved for this scale after being rejected at neighborhood scale, is unstarted. Two research questions remain deferred to a future session (standard extent sizing; curated 'other rivers' set for the state inset) - do not resolve unprompted. Basemap tile provider (currently Carto, stability risk flagged) is explicitly deferred to multi-parcel testing, not to be revisited now. Prose remains explicitly deferred - do not draft unprompted."
```

---

## Active Plan

*The living plan is `docs/WORKFLOW_SPEC.md` (the 8-step workflow) - this section tracks what's actively being worked, not a duplicate of it.*

Acquisition layer (steps 1-2) is built and validated: parcel, DEM, soils, PRISM climate, wind, flood zones, building footprints, canopy extent, watershed (HUC06/HUC12), ecoregions (EPA Level III/IV), base map roads (OSM/Overpass) — twelve sources, each with a real bug caught and fixed during validation (see `docs/DATA_SOURCE_RESEARCH.md` for the per-source detail, don't re-derive from scratch).

Translate phase (step 3/4) has been a visual-first exercise throughout, not prose-first — Peter's explicit call, since prose is deliberately held back until the writing approach itself is proven; this has not changed. Step 4's two regional/neighborhood-scale prototypes are now settled, tested, and committed: `R/illustrate/regional_inset.R` (state-scale ecoregion/river inset) and `R/illustrate/neighborhood_context.R` + `basemap_tiles.R` (neighborhood-scale contours/hydrology/watershed/parcel over a real basemap). Full design history, rejected approaches, and open risks are in `docs/ILLUSTRATION_NOTES.md` - read that before re-deriving any of this work.

Not yet started:
1. Parcel-scale illustration (step 5) — the current focus. DEM raster work (hillshade, slope, TPI) reserved for this scale.
2. Regional-orientation prose — explicitly deferred, do not draft unprompted.
3. Steps 6-8 — named and scoped at the "what must this convey" level in `docs/WORKFLOW_SPEC.md`; illustration/implementation approach deliberately not decided.

---

## Validation Checklist

*Completed during VALIDATE phase. Agent self-checks before marking any step complete.*

- [ ] Code does what the plan said it would do
- [ ] No files modified outside stated task scope
- [ ] No dependencies added without stating what they are and why
- [ ] No TODOs, placeholders, or incomplete sections left in code
- [ ] Terminal output reviewed — no silent failures
- [ ] **Project-specific:** any new raster export checked against native resolution — never request more pixels than the bbox supports (see the DEM oversampling incident, `docs/DATA_SOURCE_RESEARCH.md`)
- [ ] **Project-specific:** raw source fields preserved in acquisition functions, not collapsed/classified until translate/synthesis stage
- [ ] **Project-specific:** any new data source cross-checked against at least one known real-world fact for the test parcel (a correct watershed name, a plausible climate value, a real street name) — not just "the API responded"
- [ ] **Project-specific:** field names not assumed to mean what they suggest — check actual values before relying on a field (the `LIDAR_HAG` false lead is the cautionary example)

---

## Log

*Running record. Agent appends one entry per response. Do not delete entries.*
*Format: [TIMESTAMP] [PHASE] [ACTION] — [RESULT or OBSERVATION]*

[2026-07-22] [CONSTRUCT] Fresh start from PRD v1 — archived prior R/Quarto/Shiny attempt (`archive/pre-fresh-start-2026-07`), bootstrapped Oak+Git dual-VCS, carried forward PRD + prior data-source research.
[2026-07-22] [CONSTRUCT/VALIDATE] Parcel (NC1Map_Parcels) and DEM (NC OneMap DEM03) validated against test parcel (7 Hill St, Wake County). DEM oversampling bug found and fixed (fake grid-pattern erosion-risk artifact) — root cause: requesting more pixels than native resolution supports.
[2026-07-24] [CONSTRUCT/VALIDATE] Soils (NRCS SDA + UC Davis SoilWeb) validated; two real bugs fixed (NULL-dropping row misalignment, wrong assumed endpoint for taxonomy/hydraulic data). PRISM validated — root-caused the old repo's "empty structure" bug (normals were never on the modern REST API). Wind validated — caught a bug where naive nearest-station selection picked a precipitation-only CoCoRaHS station over the real airport station. PR #1 merged (parcel, DEM, soil, climate, wind).
[2026-07-24] [CONSTRUCT/VALIDATE] Watershed (WBD), ecoregions (EPA), base maps (OSM) source-validated — extent/styling decisions deferred to visualization stage per Peter. Flood zones (FEMA NFHL) validated and corrected twice: first, the old "404 = not in a flood zone" assumption was wrong (coverage is comprehensive, real check is `SFHA_TF`); second, "Zone X" isn't one risk picture — only 1 of 11 real subtypes is a true negative. Building footprints (NC per-county) and canopy extent (NLCD TCC) validated after reconsidering and rejecting a raw-LiDAR-point-cloud approach (Peter provided a sample `.las` file; decided it was primary geospatial analysis rather than citing an existing authoritative product, unlike everything else in this pipeline). Caught a false lead: `LIDAR_HAG` looked like building height, wasn't (ground-elevation reference). PR #2 merged (watershed/ecoregion/basemap docs, flood zones, building footprints, canopy).
[2026-07-24] [BLUEPRINT] Added workflow steps 4-8 to `docs/WORKFLOW_SPEC.md`, documented human-in-the-loop as an evidence-based build discipline (grounded in the bugs above, all caught by human review, none by the pipeline), noted the v2/v3 email-triggered-automation goal without committing to it.
[2026-07-24] [ANALYZE] Bootstrapped `project_config.md` and this file from repo docs + session history, per Peter's request to prepare a fresh-agent starting point. Confirmed sufficient information exists in `docs/` + agentmemory to do this, with named open gaps (deployment target, public-repo test-data question) rather than invented answers.
[2026-07-28] [CONSTRUCT/VALIDATE] Built `R/acquisition/watershed.R`, `ecoregion.R`, `basemap.R`. Verified live field schemas (not assumed from docs) against a real, non-address Raleigh coordinate before writing any code, then ran each function end-to-end: watershed returned HUC06 "Neuse"/030202 and HUC12 "Walnut Creek"/030202011101 (exact match to the doc's prior test-case values), ecoregion returned Level III "Piedmont"/45, Level II "Southeastern USA Plains", Level I "Eastern Temperate Forests", Level IV "45f"/Northern Outer Piedmont (also exact match), basemap returned 46 real, correctly-named local roads via Overpass. Basemap implements raw road-feature retrieval only (Overpass), not the pre-rendered-tile alternative — styling/extent decisions remain deferred to the visualization stage per existing docs. All step 4 prerequisite sources are now built; step 3 (translate) is next.
[2026-07-28] [BLUEPRINT] Output-stack conversation: reviewed an external reference report (cursor.com/insights) for typography/layout quality; agreed to stay on Quarto (R pipeline unchanged) with a custom SCSS theme rather than switching stacks — resolves one of the two previously-open items in `project_config.md` (deployment/hosting itself remains open). Began the regional-orientation image for step 4 as a visual-first translate exercise (Peter explicitly deferred prose until the writing approach is proven). Iterated a state-scale ecoregion inset through six versions: v1 showed the true unclipped EPA Level III "Piedmont" polygon, which spans AL-to-NJ and is illegible as "a region a client recognizes" — a real finding, not a styling issue. v2/v3 clipped to the NC state boundary (fetched via `tigris`, not yet a cached asset) and added all NC HUC6 basins for context. Peter redirected: watershed *boundary polygons* don't read as intuitive to a client; replaced with actual river *linework* (v4-v6). Found the NHD "Flowline - Small Scale" layer (National Map, layer 4) is built for exactly this cartographic scale; confirmed the parcel's own river (Neuse) renders as a complete, correct path. Attempting a curated "other major rivers" context set surfaced a real data problem, now documented in `docs/DATA_SOURCE_RESEARCH.md`'s new Hydrography section: the Catawba River returned only a ~40-mile fragment because its main course is dammed into named reservoirs, not classified as `GNIS_NAME = 'Catawba River'` line reaches — same shape of bug as the `LIDAR_HAG` false lead. Descoped to Neuse-only per Peter's direction ("work with what we have"); the general "other rivers" set is explicitly unresolved, not abandoned. v6 (current settled state) restored the grayscale L3/L4 ecoregion fills Peter asked to keep, fixed a real label-placement bug (v4/v5's manual offsets either collided with the state border or landed in the wrong region entirely) using `ggrepel` for repelled labels with leader lines instead of guessed coordinates. Peter confirmed v6 as the best output so far and accepted its remaining label imperfections as later-iteration polish, not blockers. Next: the neighborhood-scale main image (topo/hillshade, main streets only, HUC12 boundary as primary focus, parcel drawn as its real boundary within a to-be-defined "standard extent," not a point marker).
[2026-07-28] [CONSTRUCT/VALIDATE] Neighborhood-scale main image, v1 then v2. Before building, checked the real distance from the test parcel to its HUC12 boundary (1,039 ft) rather than guessing an extent size - chose a 2,500 ft buffer so the boundary crossing would actually be visible. v1 (hillshade + main streets + HUC12 boundary in blue + parcel as a dot) confirmed the DEM's already-documented building-footprint void-fill artifact is visually dominant at this zoom (expected, not new). Peter's v1 feedback: shaded relief needed improvement, watershed boundary shouldn't be blue (implies stream) and needed a label, primary streets (Poole Road, New Bern Avenue, Raleigh Boulevard) should be labeled if the road-class hierarchy could isolate them, parcel should be a standout color (not a dot), road weights should follow classification not arbitrary gray, and flagged that neighborhood-scale drainage likely isn't derivable from HUC codes and might need a city/county source - explicitly framed both that and the extent-sizing question as open research, not to be solved on the spot. Checked live rather than assuming: OSM tags Poole Road as `highway=secondary`, not `primary` - labeling only `primary` would have silently dropped one of Peter's three named examples, so both classes are labeled. Checked NHD's large-scale flowline layer for local drainage - zero features near the parcel, confirming (not just assuming) national hydrography doesn't cover this. Peter then supplied the City of Raleigh Hydrology FeatureServer directly, confirmed live with real local stream/ditch geometry and explicit "Walnut Creek" named segments nearby - added to `docs/DATA_SOURCE_RESEARCH.md`'s Hydrography section as a resolved (not just researched) source, flagged as Raleigh-city-specific, will need a different source outside city limits. v2 addressed every point: multi-directional hillshade (averaged three light directions, full 0-1 contrast) reads as proper shaded relief rather than one harsh single-direction shadow; HUC12 boundary redrawn dashed brown with a `halo_text`-labeled caption; road classes get a weight/darkness hierarchy instead of flat gray; primary+secondary named roads labeled with halo text (fixed a real bug from the first attempt - `st_line_sample` requires single-part `LINESTRING`, not `MULTILINESTRING`); parcel redrawn as its real boundary polygon in red; City of Raleigh hydrology layered in, split into named "Walnut Creek" (bold blue) vs. unnamed tributaries/ditches (light blue). Verified rather than assumed the named/unnamed split actually shows something: queried whether any `NAME = 'Walnut Creek'` segment falls inside this specific 2,500 ft display extent - none do, so the light-blue lines visible in v2 are a real unnamed tributary, not a bug or an untested code path. Two research questions remain explicitly open per Peter (standard extent sizing for parcels not near a boundary; curated "other rivers" set for the state inset) - not solved this session, correctly left as flagged.
[2026-07-28] [CONSTRUCT/VALIDATE] Peter's v2 reaction: not seeing shaded relief at all, unsure if that's genuinely flat terrain or a rendering miss. Checked rather than assumed - real relief across the 2,500ft extent is 121 ft (not flat), and the raw hillshade values before display stretch spanned -0.14 to 0.89 while 96% of pixels actually clustered in a narrow 0.54-0.72 band: a min/max normalization was letting a handful of outlier pixels (almost certainly the DEM's known building-void artifact) crush the real terrain signal into a sliver of the display range. v3 fixed this with a percentile stretch and added a blue TPI (topographic position index) tint for local lows, per Peter reconsidering aloud that he meant color, not just grayscale contrast. Percentile stretch worked (real relief became visible, correlated correctly with the actual hydrology line) but the blue tint did not - at a 97ft focal window it was dominated by the same building-void artifact, painting misleading blue outlines around building footprints rather than real drainage. Was mid-way through testing larger focal windows to separate valley-scale signal from building-scale noise when Peter interrupted: the whole DEM-raster approach was the wrong tool for this scale and wasn't helping communicate geomorphology, full stop - not a tuning problem. Redirected to a light vector contour base map instead, with DEM raster work reserved for a separate, more zoomed-in parcel-scale image (not built yet). v4 built this (DEM aggregated to ~25ft cells before contouring, at a 10ft interval, to avoid tracing the same artifact as jagged contour lines) - worked, but Peter's follow-up review caught three more issues, one a real correctness bug: (1) the watershed boundary line reused the clipped *polygon's* boundary, which silently included the display box's own edges as if they were real watershed edge, drawing a fake boundary segment hugging the frame - fixed by extracting the true boundary line from the unclipped HUC12 polygon first, then clipping that line, so only genuine watershed edge can ever appear; (2) the major-drainage arrows were labeled "Walnut Creek," misidentifying unnamed tributaries as the named creek itself - reworded to "flows to Walnut Creek" to indicate direction/destination without false identity; (3) wanted 2ft contours with 10ft grayscale index lines (not 10ft/tan), heavier hydrology line weight, a legend, and the stray `box()` border removed. v5 addressed all of it. Checked whether "hydrology as polygons" (an idea Peter raised, not yet acted on) is already available - City of Raleigh's Hydrology service is polyline-only, no polygon layer - flagged that the already-validated FEMA flood-zone polygons (`flood.R`) might be what he meant, asked rather than guessed.
[2026-07-28] [CONSTRUCT/VALIDATE] Peter's v5 reaction: still busy, still hard for a layperson to read, asked directly how the contours were sourced. Answered plainly - self-computed from the DEM via `terra::as.contour()`, not retrieved from an existing shapefile/basemap product, a real divergence from this pipeline's usual "cite an authoritative existing source" pattern worth naming rather than glossing over. Proposed five simplification directions; Peter withdrew "hydrology as polygons" entirely (was a thought, not a spec) and deferred the two open research questions (extent sizing, "other rivers" curation) to a future session, post-translate-phase. Directed implementation of three specific suggestions (quiet unlabeled base-map streets, hero-elements-only content, drop the legend), explicitly declined the "cut back contours" suggestion in favor of keeping full 2ft density at the lightest practical line weight with index lines distinguished by darker gray only, not weight - and flagged (not yet asking to fix) that all labels are horizontal rather than following line geometry, a known prototype limitation. v6 implemented all of it: streets rendered as one uniform light-gray unlabeled layer (all classes except pure-pedestrian footway/steps/path) rather than the classified+labeled hierarchy from v5 - flagged to Peter as a real reversal of his earlier explicit road-hierarchy preference, not silently changed; contours at uniform 0.2 lwd, index distinguished only by grey45 vs grey85; hydrology cut to just the two major reaches (dropped the many short, scattered minor segments entirely) plus their "flows to Walnut Creek" arrows; legend removed. Result reads substantially calmer and foregrounds the watershed boundary and drainage direction as the dominant signal, which is a stronger match to the original "reorient from social to ecological space" framing than any prior version.
[2026-07-28] [CONSTRUCT/VALIDATE] Peter clarified v6's "quiet base map" misread this whole exchange - he meant a real stock/pre-rendered base map product, not our own re-styled Overpass lines. Sent a deprecated Esri vector tile service (OpenStreetMap_v2) as a visual reference only; correctly self-corrected mid-build that attempting to render it was scope creep beyond what was asked and stopped, cleaned up the tangent. Checked Esri's non-deprecated raster equivalent instead: Canvas/World_Light_Gray_Base, confirmed live, purpose-built as a quiet data-forward basemap, backed by Esri (addressing Peter's stated concern about Carto's startup-stability). Built v7: fetched/mosaicked/reprojected real tiles into the working CRS (first attempt silently failed - `wrap()`'d raster needs `unwrap()` on reload, not a plain `rast()` call - caught before presenting). Result looked right but direct tile inspection showed Esri's "Base" layer bakes in street/place-name labels despite the Base/Reference naming convention implying label-free - flagged plainly rather than presenting a result that silently missed Peter's "no labels" requirement. Peter sent a second Esri link (World_Basemap_v2, also vector-tile) - attempted to render it for comparison, hit a harder wall (style loads but zero tile requests ever fire, likely a non-standard vector source definition), stopped rather than keep debugging per his earlier instruction not to sink time into this. Peter clarified the actual intent was outcome-only (light gray, no labels), not a vector-tiles preference - confirmed via direct tile pixel inspection that Esri's Base cannot deliver this (structurally a fused cache, no dynamic label toggle) and that Carto's `light_nolabels` raster tile genuinely can (confirmed empty of any text by direct visual check). Built v8 on Carto instead: same proven tile-fetch/mosaic/reproject pipeline (caught and fixed a second bug - Carto's tiles are 8-bit palette-indexed PNGs, `rast()` read them as 1-band; needed `colorize(r, 'rgb')` before merging or `plotRGB` would have failed), contour lines darkened (grey85/45 -> grey55/20) per Peter's request since they were disappearing into the basemap tone in v7. Reintroducing Carto is a real, not-yet-resolved tradeoff against Peter's own earlier stability concern - flagged, not silently decided. The ad hoc tile-fetch code (used for both Esri and Carto attempts) is not yet a committed `R/acquisition/*.R` function.
[2026-07-28] [CONSTRUCT/VALIDATE] Peter called a pause on regional/neighborhood-scale iteration and asked for consolidation: document risks/dependencies, decide where in `docs/` this belongs, capture working scripts for reproducibility, before shifting focus to parcel-scale illustration (his stated real goal - regional/neighborhood scale was groundwork). Created `docs/ILLUSTRATION_NOTES.md` as the translate/illustrate-layer counterpart to `docs/DATA_SOURCE_RESEARCH.md` (sources) - placed the Carto/Esri basemap-provider finding in both: a dated entry in `DATA_SOURCE_RESEARCH.md` under the OpenStreetMap section (it directly resolves that section's long-open "self-styled vs. tile provider" question) and a fuller risk writeup in the new doc. Promoted the ad hoc prototype code into three tested `R/illustrate/*.R` files (`basemap_tiles.R`, `regional_inset.R`, `neighborhood_context.R`), generalizing what had been hardcoded to the test parcel - ecoregion labels now read from `eco$level3$US_L3NAME`/`level4$US_L4NAME` instead of literal "PIEDMONT" strings, and the "flows to Walnut Creek" / "Walnut Creek watershed boundary" labels now read from `huc12$name`, not a hardcoded creek name. Caught two real bugs while writing the committed versions, not present in the working prototypes because they'd never been re-run from a clean function: `regional_inset.R` initially dropped the `st_transform(nc, 4326)` that made intersections work, and the river layer was being added to the ggplot after the label layer (would have drawn on top of, not under, the text). Both fixed and re-verified by actually re-running each script end-to-end and confirming output matched the original prototypes pixel-for-pixel (allowing for the label-text changes). Updated `project_config.md` (Key Files, Architecture) and `docs/WORKFLOW_SPEC.md` (step 4 marked prototyped-and-paused, step 5 redefined as parcel-scale and now the active focus) to reflect the phase shift.
